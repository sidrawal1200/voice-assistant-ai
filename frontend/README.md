# Frontend — Voice Assistant Portal

This is a Vite + React + TypeScript frontend scaffold using Tailwind CSS.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

The UI is in demo mode by default and does not require backend connectivity.
